/**
 * @description Main entry point for MikroChat frontend.
 * This module bootstraps the application by initializing all listeners.
 */

import { initializeListeners } from './listeners.mjs';

// Initialize all event listeners when the module loads
initializeListeners();
